{
  "name": "stylesheet-ui",
  "version": "0.0.9",
  "description": "Copy-paste React Native components for Expo. Plain StyleSheet, no DSL — you own the source. Run `npx stylesheet-ui init`, then `add <name>` to drop a component into your project.",
  "keywords": [
    "expo",
    "react-native",
    "components",
    "ui",
    "design-system",
    "stylesheet",
    "cli"
  ],
  "homepage": "https://stylesheet-ui.dev",
  "bugs": {
    "url": "https://github.com/UltimateStarCoder/stylesheet-ui/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/UltimateStarCoder/stylesheet-ui.git",
    "directory": "packages/cli"
  },
  "license": "MIT",
  "author": "Mac-Rufus Umeokolo",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "stylesheet-ui": "dist/index.js"
  },
  "files": [
    "dist",
    "registry",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format cjs --target node20 --clean --shims --no-splitting",
    "dev": "tsup src/index.ts --format cjs --target node20 --watch --shims --no-splitting",
    "sync": "tsx scripts/sync-registry.ts",
    "prepare": "npm run sync && npm run build",
    "prepublishOnly": "npm run sync && npm run build",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "commander": "^12.1.0",
    "fs-extra": "^11.2.0",
    "picocolors": "^1.0.1",
    "prompts": "^2.4.2",
    "semver": "^7.6.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.11.0",
    "@types/prompts": "^2.4.9",
    "@types/semver": "^7.5.8",
    "tsup": "^8.0.0",
    "tsx": "^4.7.0",
    "typescript": "~5.9.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
