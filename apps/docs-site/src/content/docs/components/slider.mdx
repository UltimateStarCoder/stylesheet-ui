---
title: Slider
description: Drag-to-set value. No native modules, no Reanimated.
---

import ComponentPreview from '../../../components/ComponentPreview.astro';

<ComponentPreview slug="components/slider" />

## Install

```sh
npx stylesheet-ui add slider
```

## Usage

```tsx
import { useState } from "react";
import { Slider } from "@/components/ui/slider";

const [volume, setVolume] = useState(40);

<Slider value={volume} onValueChange={setVolume} min={0} max={100} />

<Slider
  value={rating}
  onValueChange={setRatingLocal}
  onSlidingComplete={saveRating}
  min={1} max={5} step={1}
/>
```
